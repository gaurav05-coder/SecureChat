import express from 'express';
const router = express.Router();
// TODO: IPFS gateway auth endpoint (if needed)
router.post('/ipfs-auth', async (req, res) => {
  // Validate access
});
export default router;
